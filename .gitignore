# Terraform state — usar backend remoto (S3)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
# tfvars com segredos — apenas o .example vai pro repo
terraform.tfvars
!terraform.tfvars.example
# Binários
*.deb
*.rpm
*.pkg
# Java / Maven
app/target/
*.class
*.jar
# IDE
.idea/
*.iml
.vscode/
.claude/